# XiHan.Framework.Application.Contracts

## 项目定位

`XiHan.Framework.Application.Contracts` 承载应用层对外暴露的稳定契约。

## 责任边界

本项目负责：

1. 应用服务接口
2. 应用请求与响应模型
3. 稳定的跨层调用契约

本项目不负责：

1. 应用服务实现
2. 领域规则实现
3. 基础设施实现

## 依赖方向

本项目只依赖共享模型，不依赖应用实现或宿主实现。

## 当前阶段

当前项目已建立应用服务契约入口，后续将逐步补齐请求、响应和统一应用服务约定。
