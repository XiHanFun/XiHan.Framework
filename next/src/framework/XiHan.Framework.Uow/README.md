# XiHan.Framework.Uow

## 项目定位

`XiHan.Framework.Uow` 用于提供工作单元的统一抽象，协调事务边界和提交时机。

## 责任边界

本项目负责：

1. 工作单元抽象
2. 提交与回滚语义
3. 与领域事件、仓储协调的基础扩展点

本项目不负责：

1. 具体 ORM 事务实现
2. 具体数据库连接管理实现

## 依赖方向

本项目只提供统一语义，不依赖具体数据访问实现。

## 当前阶段

当前项目已建立最小工作单元抽象，后续将继续补充回滚、事件收集和事务协作能力。
