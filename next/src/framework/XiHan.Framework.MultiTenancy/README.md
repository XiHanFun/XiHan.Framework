# XiHan.Framework.MultiTenancy

## 项目定位

`XiHan.Framework.MultiTenancy` 提供多租户能力的默认实现。

## 责任边界

本项目负责：

1. 当前租户上下文默认实现
2. 多租户服务注册扩展
3. 为上层宿主与模块提供统一接入点

本项目不负责：

1. 具体租户管理模块
2. 数据访问层租户过滤实现细节

## 依赖方向

本项目依赖多租户抽象，不直接承担业务层租户管理职责。

## 当前阶段

当前项目提供默认上下文实现，后续将继续补充服务注册扩展和宿主接入能力。
