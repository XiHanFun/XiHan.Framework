# XiHan.Framework.Security.Abstractions

## 项目定位

`XiHan.Framework.Security.Abstractions` 定义身份、安全主体和权限检查相关的基础抽象。

## 责任边界

本项目负责：

1. 当前用户抽象
2. 权限检查器抽象
3. 安全上下文基础契约

本项目不负责：

1. 身份认证协议实现
2. 授权中间件实现
3. 具体安全存储实现

## 依赖方向

本项目只定义抽象，不依赖宿主和认证实现。

## 当前阶段

当前项目已建立当前用户抽象，后续将继续补充权限检查器和安全上下文约定。
