# XiHan.Framework.Authorization

## 项目定位

`XiHan.Framework.Authorization` 提供统一授权检查入口和默认实现骨架。

## 责任边界

本项目负责：

1. 授权检查器默认实现骨架
2. 授权服务注册扩展
3. 为模块和宿主提供统一权限检查入口

本项目不负责：

1. 认证协议本身
2. 具体权限存储和管理模块
3. 宿主层授权中间件细节

## 依赖方向

本项目依赖安全抽象层，不直接承担认证职责。

## 当前阶段

当前项目已建立授权接入骨架，后续将继续补充权限检查器、策略模型和宿主装配扩展。
