name: CI

on:
  push:
    branches: [main, 'release/*']
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - run: dotnet restore XiHan.Next.slnx
      - run: dotnet build XiHan.Next.slnx --no-restore -c Release
      - run: dotnet test XiHan.Next.slnx --no-build -c Release --verbosity normal
